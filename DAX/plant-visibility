Production Lbs (Plant Visibility) :=
VAR currentDate =
    MAX('dimDate'[Date])

VAR plantActiveFrom =
    MAX('dimPlant'[plantActiveFromDate])

RETURN
IF(
    currentDate < plantActiveFrom,
    BLANK(),
    [Production Lbs]
)
